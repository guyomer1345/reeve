# Directives — standing operator instructions about how this loop behaves

<!-- ADDING ONE (validated by `.claude/scripts/check_directives.py` on every commit):
     `## <title>`, then `- key: value` lines, then the body.
       type      mechanized | behavioural
       entered   YYYY-MM-DD
       retire    on:YYYY-MM-DD (expires) | when:<path> (retires the day that mechanism exists) | standing
       mechanism required iff mechanized, forbidden otherwise — `<path>` or `<path>#<dotted.key>`
     MECHANICAL-FIRST: if a script could evaluate the trigger, WIRE it and index it here in ONE
     line saying what it achieves. Prose is the fallback, never the default — this file is read
     before every turn, so it is hard-budgeted; an entry that stays is rent every session pays. -->

## The autonomy boundary
- type: behavioural
- entered: 2026-09-13
- retire: standing

Take every decision that does not change the goal; route anything that may. Goal-affecting ≈ would
change a `locked` spec element, or alter what an acceptance criterion demands. The mechanical floor
that judgment may escalate above and never below is computed, not restated here — see
`shared/schemas.md § the autonomy floor`.
